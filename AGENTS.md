# TangTool 协作与接续说明

> 更新于 2026-09-14。本文只保存当前约定、状态和下一步；历史实现过程请查 Git 记录。不要从早期占位页或已完成里程碑重新开始。

## 1. 目标与协作约定

TangTool 是 Windows / macOS 本地桌面工具箱，强调离线处理、隐私和直接的操作体验。开发者熟悉 Vue / TypeScript，正在学习 Tauri、Rust、Tailwind CSS、文档转换和跨平台发布。

- 小步结对开发：任务开始说明目标、原理与验收标准；实现后解释关键点、验证并更新本文。
- 需求存在实质歧义时先提问；低风险细节可说明假设后继续。需要后续确认的位置用具体 TODO 标注。
- Vue / HTML 标签属性尽量同一行，过长才换行；不要把整个组件挤成一行。
- 通用可读性：不压缩代码，不把多条语句、复杂逻辑或完整代码块挤在同一行；合理保留缩进、空格与空行。
- 不拆分只有一行且只调用一次的小方法；为业务规则、边界和非显然原因写有价值的注释。
- 除非用户明确要求，不新增或遗留单元测试文件。可用临时脚本验证后删除；不要顺手改动已有测试基线。
- Tailwind 变更应解释类名对应的 CSS 与布局原理，保留学习机会。
- 不擅自调整工具栏按钮顺序或放大按钮；使用紧凑按钮和 Lucide 图标。
- 不提交密码、令牌、私钥、个人文档。保留不属于本任务的改动。
- **保留用户的 `.venv` 和 `.venv-sidecar`，不能作为临时文件清理，也不能跨机器复制环境。**
- 提交、推送、发布和仓库设置变更按用户授权执行；换机前确保需要的改动已提交并推送。

## 2. 技术与边界

- Tauri 2 + Rust；Vue 3 + TypeScript + Vite 6；Pinia、Vue Router 4、Tailwind 4、Lucide。
- CodeMirror 6：JSON 编辑、折叠、语法高亮和错误定位；microdiff：结构化对比。
- SheetJS 0.20.3 使用官方发布源；不要替换成 npm 上过时的 0.18.5。
- MarkItDown 0.1.7 + PyInstaller 6.22.3；跨平台依赖及哈希固定在 `sidecar/requirements.lock`。
- Vue Router 5 与当前 Vite 不兼容，不使用 `--force` 绕过依赖检查。
- 工具内容默认仅在本机处理；前端做轻量文本操作，Rust 做文件、密码随机源及进程隔离。
- 路由懒加载 + 以路由名为 key 的 KeepAlive：菜单切换保留工具输入，刷新/关闭应用后清空。
- 仅外观与侧栏偏好写入 `tangtool.preferences.v1`；不持久化 JSON、密码或文档内容。
- 文件选择/保存使用 Tauri Dialog + 最小 FS 权限；不开放任意目录遍历和删除能力。
- 应用标识：`com.github.liuxiangyu2026.tangtool`；默认 1200×800，最小 900×640。

## 3. 已实现功能与不可丢失的规则

| 模块 | 当前能力 / 规则 |
| --- | --- |
| JSON 格式化 | 单编辑器；示例→格式化→压缩→复制→清除→导入→导出；实时校验、行列定位、折叠与彩色语法 |
| JSON 对比 | 新增/删除/修改，点击差异定位；数组按索引，不识别移动；对齐以左侧共同 key 顺序为准，独有 key 各自置后，不补字段、不重排数组 |
| JSON 转 Excel | 嵌套对象展开；null 为文本 `null`、缺失留空；数组默认紧凑 JSON，可选独立 Sheet 并重复主要字段；预览 50 行、保存全部 |
| 树形 JSON 平铺 | 检测后代同名数组字段才提供选项；每条根到末级路径一行，保留所有父级数据；单 Sheet，不做笛卡尔展开 |
| 拖动布局 | JSON 对比左右 25%～75%、结果高度 20%～60%；Excel 左右 25%～75%；文档左右 25%～55%；统一 Grip 图标并支持键盘 |
| 密码 | Rust 操作系统安全随机源，无偏采样与打乱；长度 4～128（默认 20），数量 1～20（默认 6）；保证所选字符类型，默认排除易混淆字符 |
| URL / MD5 / Base64 | 三个独立菜单及不同图标；URL 参数组件编解码；MD5 文本/文件；Base64 文本/文件；文件 Rust 流式处理；明确不是加密 |
| 文档转 Markdown | 仅 DOCX / PDF，不支持 .doc、无需 LibreOffice；最多 10 个，追加并按路径去重；逐个转换、点击查看、独立删除与保存；清除需确认，只清列表不删文件 |
| Markdown 预览 | marked 解析；格式化 / 原始模式，支持标题、表格、列表等；复杂排版、扫描 PDF、字体可能影响转换，不含 OCR |
| 侧栏与设置 | 图标折叠、悬停名称，底部版本/设置入口；浅色/深色/跟随系统；紧凑/标准/较大字号；本机记忆 |
| 一级菜单 | 分组标题可展开/收起；底部“折叠到一级 / 展开全部分组”；collapsedGroups 与外观一同保存；图标侧栏模式继续显示工具图标 |
| 时间戳 / 时区 | 明确秒/毫秒；ISO 日期输入必须带 Z 或偏移；支持常用 IANA 显示时区；拒绝非法日期，不猜测时区 |
| UUID | 安全随机 UUID v4；1～1000 条、默认 10；大小写/连字符选项；批量复制，不写入磁盘 |
| 正则调试 | JavaScript g/i/m/s/u/y 标志；Worker 执行，1 秒超时；最多 1000 匹配，有长度/捕获组限制，处理 Unicode 零长度匹配 |
| 文本整理 / SHA | 逐行去首尾空白→移除空行→区分大小写去重→字符排序；SHA-256/512 按 UTF-8 计算，支持空文本，最多 2 MiB；SHA 本轮只支持文本 |
| 官网 | `website/` 独立静态站：功能、平台下载、版本和限制说明；GitHub Pages 手动发布工作流，尚未上线 |
| 首页 | 默认 `/`，菜单名“首页”；顶部单线搜索，无介绍文字；中文/英文/拼音及非连续模糊匹配；共用 `src/data/tools.ts`，会话内保留搜索，不落盘 |
| Excel/CSV 转 JSON | XLSX/XLS/UTF-8 CSV；单表数组或多表分组；空单元格 null、空/重名表头补齐；原始/显示文本模式，不执行公式和宏；完整只读 CodeMirror、行号/高亮/折叠/全部展开及完整复制 |
| Cron | 用户确认仅标准 5 段；中文说明，IANA 时区、ISO 起点、未来 10 次；日期/星期同时限制时 OR；不支持 Quartz、不创建任务 |
| QR Code | 本地文本/网址，PNG/SVG 导出；默认 M/512px、4 格白边、最多 1000 UTF-8 字节，不访问输入 URL |
| 文本对比 | 逐行新增/删除高亮与行号；忽略行内空白但保留换行，统一 CRLF/LF；每侧 2000 行/20 万字符，后台超时保护 |
| 图片压缩 | 用户确认单张 JPEG/PNG/WebP → JPEG/WebP；质量/最长边、前后预览和体积；JPEG 透明区铺白，动画只留首帧，不保留元数据，默认 `_compressed` 另存 |
| 颜色选择器 | `/color`；二维 S/V 色板+竖向 H 条，点选/拖动/方向键；完整 216 安全色独立滚动、常用色前置、色块铺满并显示 HEX；通道/透明度/多编码/文字对比度保留 |

设置当前范围：主题/界面字号、工具收藏、窗口尺寸/最大化和分栏记忆、编辑器字体/换行/2-4-8空格缩进、默认导出目录及原名/日期时间后缀。无账户同步、自动更新或任意字体下载；提示时长仍固定 2 秒。只保存偏好与布局，不保存密码/JSON/文档内容。

## 4. 打包与发布真实状态

- M0～M4 核心功能已实现；M5 页面和独立 sidecar 已实现；目前进入 M6 发布准备及体验增强。
- GitHub Actions [运行 34800140359](https://github.com/liuxiangyu2026/TangTool/actions/runs/34800140359) 的 Windows x64、macOS ARM、macOS Intel 构建、sidecar health 和产物上传全部通过（构建提交 `107b4be`）。
- 产物名：`TangTool-x86_64-pc-windows-msvc`（含 NSIS .exe）、`TangTool-aarch64-apple-darwin`、`TangTool-x86_64-apple-darwin`（macOS 为保留权限的 .app ZIP）。
- macOS ARM sidecar 与 Rust 打包路径通过隔离目录、无有效 Python 搜索路径的转换验证。**CI 成功不等于无开发环境的干净机器验收成功。**
- Windows/Intel macOS 安装运行、无 Python/无仓库/离线转换、升级与卸载仍待真机验收。本机此前 GUI 验收受锁屏/屏幕捕获限制，不能据此标记通过。
- 本轮开始时仓库没有公开 Release。当前应用版本 0.1.0，仍标记开发阶段；未完成签名、公证和自动更新。
- 完整安装包内置 Python、MarkItDown 和依赖；最终用户不需要安装这些运行时，但 Windows WebView2 离线安装策略仍待完善。
- 开发转换使用项目 `.venv`；打包应用只调用主程序同目录 sidecar，不回退系统 Python。通信 UTF-8 JSON、后台执行、180 秒超时和输出大小限制。
- 完整安装包必须使用 `npm run desktop:build`；普通 `tauri build` 不带 sidecar 配置，不能当作完整交付包。

## 5. 当前任务、验收与下一步

本轮：补齐颜色对比度底色与合成预览，侧栏滚动条收窄、折叠 Logo 顺序修正。Word/PDF 互转仍暂缓。

- 原因：底色仅用于数值计算、未绘制预览；默认 alpha=1 时底色被完全覆盖，切换后数值不变属于正确行为。增加底色小样/HEX、黑白字叠加预览及不透明度说明，不擅自改变 alpha。
- 侧栏菜单 WebKit 滚动条 4px，其他引擎使用 thin；仅作用于 #tool-navigation。折叠模式 Logo 移到展开按钮上方，展开模式保持品牌区布局。
- 已验证：前端构建、现有 13 个测试、git diff --check；临时验证 opaque/半透明/全透明对比度边界。浏览器确认 #33669980 黑白底切换时预览/数值变化且 RGBA 不变、侧栏滚动条4px、Logo位于按钮上方。未新增测试文件。

- 收藏在首页星标与设置页管理，持久化工具路径，首页收藏优先及仅收藏筛选。`usePanelRatio` 覆盖通用 SplitPane 及旧 JSON 对比/转 Excel/文档分隔条，按唯一 key 保存比例，关闭记忆清除历史。
- `useWindowPreferences` 仅在 Tauri 中恢复/记录逻辑尺寸与最大化；最小化不覆盖记录，最大化保留普通尺寸，按当前屏幕限制恢复尺寸。增加 core:window:allow-set-size / allow-maximize 权限，无新依赖。浏览器不调整浏览器窗口。
- CodeMirror 各实例用 Compartment/插件监听换行和缩进，销毁取消监听；字体经 CSS 变量覆盖代码与 textarea。JSON 格式化/对齐/表格转 JSON 新生成结果遵循缩进，原内容不自动重写。
- `exportDefaults` 为所有已接入工具的 save 对话框统一提供目录/原名或日期后缀；浏览器只应用命名。目录需桌面 Dialog 选择；保留系统确认和既有文件权限，不自动创建目录/写文件。
- 色板与编码区增大行距并填满上方，顶区 386～480px，额外高度归安全色。卡片按 auto-fill 自动增列、aspect-ratio 3:2；1920×1080 实测比例 1.5、顶区480px、安全色可视351px。
- B 图标已覆盖 public/tangtool.svg、website/mark.svg、现有桌面PNG/ICO/ICNS。Tauri本地源码确认 macOS dev Ready 会读取应用图标，打包模式使用同一套 bundle.icon；尚未重新交付安装包。
- 验证：前端/官网构建、13 个现有测试、Cargo check；隔离验证偏好迁移/非法值/收藏/导出路径命名、窗口恢复/最大化/最小化/清理、CodeMirror缩进及分栏恢复。生产预览验证Menlo、自动换行、4空格格式化、收藏刷新保留、分栏75%刷新恢复、大窗口色卡比例及B品牌显示。无永久测试文件。
- 原生启动因端口占用及旧安装版窗口干扰，开发进程编译运行成功，但未完成本轮原生窗口跨重启尺寸/目录选择、Dock/任务栏或安装包验收，不能用浏览器结果替代。未关闭用户旧安装版；临时进程已结束。

- `ColorPlane` 模仿用户截图的 HSV 二维色板+竖向色相条；点击/拖动/方向键，Shift 10% 步进；黑/灰色保留内部色相，避免拖动回写 RGB 时跳点。不透明度保持不变。
- 下方 216 张安全色卡完整填色、黑/白文字自适应，18 个常用色排前；列表固定在剩余空间、独立竖向滚动，上方选色和编码不随列表滚动。点击卡片恢复不透明。
- Logo 历史曾暂定 C，最新已改 B；通过官方 icon 命令生成桌面资源，没有加入移动端资源。系统图标需重编译/重启确认。
- 已临时验证 HSV 216色往返、色相边界、黑/白/灰及 alpha；浏览器确认底部安全色点击后上方位置不变、HEX更新，色板点选。更多设置建议：启动页/工具收藏排序/窗口与分栏记忆/编辑器字体与换行缩进，第二批导出目录命名/提示时长/快捷键；不默认保存敏感工具输入。
- 本轮前端/官网构建、现有 13 个测试、cargo check --offline、git diff --check 通过；已检查 C 的 PNG/ICO/ICNS 资源及浏览器窗口内品牌显示、侧栏折叠后 Logo 保留。尚未完成 Windows/macOS 系统任务栏/Dock 实机图标验收。

- Logo 候选在 `design/logo-candidates/`：A 折角 T、B 模块 T、C 随身工具箱、D 双 T 协作；512×512、具名路径分组、无位图/字体依赖。当前选择 B，未进行商标检索。
- 浏览器禁止打开 file:// 对比页，未绕过该限制；使用本地 SVG 渲染器检查 PNG 总览和小尺寸效果，用户可自行打开 HTML 或查看 PNG。
- 颜色“安全色”按 Web 安全色解释：RGB 通道为 51 的倍数且不透明。推荐色不代表任意搭配都可读；对比度先按明确黑/白底合成透明色，再按 WCAG 相对亮度计算，普通文字 AA 4.5:1 未舍入判定。HSV 仅数值参考，不标成 CSS 函数。
- 颜色默认 #336699；安全色点击恢复不透明，HSV 选色保持 alpha；结果与输入通过 KeepAlive 保留，不写磁盘。不增加依赖、Rust 或权限。
- 已验证：前端/官网构建、现有 13 个测试、git diff --check、SVG XML 检查；临时验证 HEX 各种长度/透明、主色与灰度 HSL/HSV、对比度黑白/透明边界、216 色唯一及推荐色归属、非法输入。浏览器确认色卡/推荐色点击、透明 HEX、编码联动、非法输入不改旧颜色与切页保留。未新增测试文件；未读取用户剪贴板，原生取色器/桌面复制仍待真机复核。

- `ToolNotice` 固定在视口中上方（8vh），状态/错误均显示 2000ms，新消息重新计时；离开页面与卸载清理定时器，返回页面不重播旧提示。
- 首页搜索桌面宽度为内容区 60%，居中，上方留白 48px，与结果统计间隔 40px；窄屏恢复全宽。
- 新增通用 `SplitPane`：默认 25%～75%，Pointer capture、方向键 2% 步进、Home/End 极值、ARIA；比例随 KeepAlive 保留，停止拖动清理捕获。Grid minmax(0, fr) 防长内容撑大面板，内部独立滚动。
- 左右拖动补齐 URL/MD5/Base64、文本整理、二维码、图片压缩、密码设置/结果；正则、SHA、表格转 JSON 上下拖动；文本对比同时上下/左右拖动。已有 JSON 对比/转 Excel/文档分隔条不改。
- 已验证：前端构建、现有 13 个测试、git diff --check；文本对比鼠标与键盘拖动、结果出现前后面板尺寸稳定；图片分栏 75% 边界及正则上下调整；首页搜索占内容区约 60%；2000ms计时、更新重置、停用/卸载清理经临时隔离验证，未新增测试文件。桌面跨平台拖动仍需真机复核。
- Logo 历史决定：内置图片生成工具不可用，用户选择 SVG；4 个候选已完成，当前 B 已接入窗口。

- 全部已接入路由的工具提示统一到 `ToolNotice` 中上方固定浮层，2 秒自动消失，也可手动关闭；KeepAlive 停用时隐藏，不撑高工具栏/挤压编辑器。
- `JsonResultEditor` 显示完整结果（不再截断 10 万字符），与格式化页共用 `jsonEditorAppearance`。根级折叠直接使用合法 JSON 边界，避免等待大文档语法树；复制与保存始终使用完整结果。
- 已实测图片页和 URL 页提示前后面板位置/高度相同；首页 focus 只有 1px 底线、无 outline/box-shadow；3000 行 JSON 只渲染可见行，根级折叠/展开正常，切页无旧提示串页。
- 完整复制在隔离模拟中覆盖浏览器/桌面分支、271892 字符/3000 条记录、空内容与拒绝权限。原剪贴板备份读取被权限审核拦截，未读取系统剪贴板，也未绕过；真实系统复制仍待用户实测。

- 首页和侧栏共用工具目录，当前 19 个工具；底部折叠、版本、设置均为 text-xs、14px 图标。
- 新增 qrcode、cron-parser、cronstrue、diff 与 @types/qrcode；重型表格/Cron/文本差异放 Worker，按路由懒加载。
- 表格最大 10 MiB/50 Sheet，每表 10000 行数据/200 列、总 20000 行；超过边界拒绝导出，编辑器虚拟滚动展示完整结果。CSV 强制 UTF-8 保留前导零。
- 图片限制 20 MiB/2400 万像素，质量 10～100、最长边 64～8192；输出可能更大时提示。没有 PNG 无损压缩或批量处理。
- 已验证：前端/官网构建、现有 13 个测试、git diff --check；临时检查表格多表/空表/重名/中文 CSV/行上限，Cron DST 与日星期 OR，文本空白/换行以及搜索规则。浏览器确认首页搜索、表格文件选择/多 Sheet 结果、二维码预览、JPEG/WebP 质量与缩放、Cron 未来 10 次、文本逐行高亮。临时样本验收后清理，未新增测试文件。
- 本轮无 Rust/权限变更：新文件输入用 WebView 本地选择，导出复用 Dialog/FS。Windows/macOS 安装包内文件选择、导出和图像编解码仍需真机验收；浏览器验证不能替代。
- 依赖安装报告 2 项 moderate 风险。npm audit 被权限审核拦截（会向 npm Registry 发送依赖名称/版本），已向用户询问授权，未绕过；详情尚未核实，不能声称无漏洞。
- 官网和版本说明同步本轮工具，但尚未部署或发布 Release。保持之前已实现功能及开发环境。

优先后续任务：

1. 复查本轮 UI 并完成目标机验收，修复发布前风险。
2. 确认应用图标、签名/公证计划和 Windows 离线 WebView2 策略。
3. 创建公开 Release、上传已验收的三平台安装包；启用 Pages 并手动发布官网，具体见 `website/README.md`。
4. 接入文档原生拖放（当前仅文件选择，拖放尚未实现）。
5. 按用户确认逐步增加工具，不一次性扩展全部候选。

待评估/修复的质量事项（不表示本轮已修复）：

- Markdown 渲染为 HTML 后尚未做专门净化，需要审查预览安全边界。
- Base64 文件输出应审查同路径覆盖、失败残留与最终 flush 错误处理。
- 文档转换期间选择/删除、保存对话框期间切换文档的状态一致性。
- ESLint / Prettier、质量检查工作流仍待补齐；测试策略遵守第 1 节。

新增工具剩余候选（未实现、待确认）：JSONPath、SQL 格式化、JWT 本地查看（解码不等于验签）、SHA 文件摘要、JSON/YAML；Word/PDF 互转用户已明确暂缓。坚持本地处理，不默认增加云端 API、账户或上传。

## 6. 换机与常用命令

先阅读本文、`README.md`、`sidecar/README.md`；以 Git 状态与最新提交为准，不在本文复制不断失效的“最新 HEAD”。

```bash
git status --short --branch
git log -1 --oneline
# 工作区干净且已确认接续分支时：
git pull --ff-only
npm ci
npm run tauri dev
```

- Windows：Node.js LTS、Rust stable、MSVC Build Tools、Windows SDK、WebView2。
- macOS：Node.js LTS、Rust stable、Xcode Command Line Tools。
- Python 开发/打包环境按 `sidecar/README.md` 在新电脑重建；保留已有环境，不从其他电脑复制。
- CI 使用 Node 22、Python 3.10 和 Rust stable；首次依赖下载与 Rust 编译可能较慢。
- 前端热更新：`npm run tauri dev`；只看网页布局：`npm run dev`（不能验收 Tauri 文件和密码能力）。
- 官网：`npm run site:dev`；生产构建：`npm run site:build`；发布说明：`website/README.md`。
- 常用检查：`npm run build`、现有 `npm test`、`git diff --check`；Rust 变更再运行 Cargo check / Clippy / rustfmt。
- 打包：`npm run desktop:build`；三平台工作流：`.github/workflows/desktop-build.yml`（手动触发）。
